import 'package:flutter/material.dart';

// Padding and Spacing
const double kPaddingS = 8.0;
const double kPaddingM = 16.0;
const double kPaddingL = 24.0;
const double kBorderRadius = 12.0;

// Animation Durations
const Duration kAnimationDuration = Duration(milliseconds: 300);

// Text Styles (can be expanded in theme.dart)
const TextStyle kTitleTextStyle = TextStyle(fontSize: 20, fontWeight: FontWeight.bold);
const TextStyle kSubtitleTextStyle = TextStyle(fontSize: 16, color: Colors.grey);

// Other constants
const double kProductCardAspectRatio = 0.75;
